@import '../../bootstrap.styl'

.jumbotron
  +has('subtitle')
    display: block
    font-size: 2.5rem
    font-weight: 100
    text-align: center

    +variant('dark')
      color: $primary_white

    +variant('light')
      color: $primary_black
