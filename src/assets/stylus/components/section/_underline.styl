@import '../../bootstrap.styl'

.section
  +has('underline')
    display: block
    height: 2px
    margin: 1.5rem auto
    width: 200px

    +variant('dark')
      background-color: $primary_dark

    +variant('light')
      background-color: $primary_light
